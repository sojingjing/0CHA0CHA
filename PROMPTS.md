# 단계별 개발 프롬프트

`PRD.md`를 기반으로 구현을 4단계로 나눈다. 각 단계는 독립적으로 실행 가능한 프롬프트로, 이전 단계 산출물 위에 이어서 작업한다.

---

## 1단계 — 기본 뼈대

```
PRD.md를 기준으로 프로젝트 기본 뼈대를 만들어줘.

- requirements.txt: streamlit, finance-datareader, openai, python-dotenv,
  pandas, networkx, plotly 포함
- .env: OPENAI_API_KEY 플레이스홀더 포함, 실제 키는 사용자가 채워 넣도록 안내
- app.py:
  - python-dotenv로 .env 로드, OpenAI 클라이언트 초기화(키 없으면 UI에 경고만
    표시하고 죽지 않도록)
  - fdr.StockListing('KRX')를 st.cache_data로 캐싱하는 종목명↔코드 조회 함수
  - 종목코드로 최근 N거래일 주가를 가져와 등락률(%)을 계산하는 함수. fdr 호출
    루프에는 반드시 time.sleep(1)을 넣을 것
  - LLM/GraphRAG 없이, 사용자가 쉼표로 구분한 종목명을 직접 입력하면 코드
    매핑 → 등락률 조회 → 표로 보여주는 최소 Streamlit 화면 (다음 단계에서
    LLM 파이프라인으로 교체될 자리표시자)
  - 종목명이 KRX 목록에 없거나 가격 조회 실패 시 해당 종목만 건너뛰고 에러
    문구를 표에 남길 것 (전체 프로세스 중단 금지)
```

## 2단계 — 핵심 기능(GraphRAG + LLM 연동)

```
1단계 app.py에 PRD §4(F2~F7), §9의 GraphRAG 파이프라인을 추가해줘.

- OpenAI Chat Completions(response_format=json_object)로 이슈 텍스트를 입력받아
  {theme, lead_stock, nodes:[{name, stage, reason}], edges:[{source, target,
  relation}]} 형태의 JSON을 반환하도록 시스템 프롬프트 설계
- 반환된 JSON을 안전하게 파싱(실패 시 사용자에게 원문과 함께 에러 표시)
- networkx로 그래프를 구성하고 lead_stock을 root로 BFS hop 거리를 계산해
  0=대장주(1차), 1=2차, 2=3차 이상으로 태깅
- 각 node.name을 1단계에서 만든 KRX 종목명↔코드 매핑 함수로 검증/매핑
  (매핑 실패 시 '미확인 종목'으로 표시하고 가격 조회는 건너뜀)
- 매핑된 각 종목의 등락률을 조회(sleep(1) 유지)하고, 등락률 ≥ 임계값(기본
  15%, 사이드바에서 조정 가능)인 종목은 '이미 급등' 상태로 표시하되
  추천 리스트에서는 제외
- 대장주가 이미 급등한 경우를 우선 감지하고, 남은 2차/3차 종목을 hop 거리
  기준으로 정렬해 추천 리스트로 노출
- 수동 종목 입력 자리표시자는 이 LLM 파이프라인으로 교체
```

## 3단계 — UI/시각화

```
2단계 로직 위에 PRD §7(화면 설계)에 맞춰 Streamlit UI를 완성해줘.

- 사이드바: 이슈 입력, 급등 임계값 슬라이더(기본 15%), 조회 기간 선택,
  분석 시작 버튼
- 메인 화면 상단: 이슈 요약 + LLM이 설명한 밸류체인 텍스트
- networkx 그래프를 plotly로 시각화: 대장주는 강조색, 이미 급등한 종목은
  회색/비활성, 추천 후보는 하이라이트색, hop 거리에 따라 레이어 구분
- 결과 테이블: 종목명 | 코드 | 단계(1차/2차/3차) | 관계 설명 | 등락률 |
  상태(추천/제외-이미급등/미확인)
- 추천 종목 카드: 근거 텍스트(LLM reason) 표시
- CSV 다운로드 버튼
- 진행 상태 표시(st.spinner/progress) — sleep(1)로 인한 지연을 사용자가
  인지할 수 있도록
- 하단에 "참고용 정보이며 투자 판단은 본인 책임" 문구 고정 표시
```

## 4단계 — PRD 정합성 및 버그 리뷰

```
완성된 app.py를 PRD.md의 기능 요구사항(F1~F11)과 비기능 요구사항 체크리스트로
한 줄씩 대조 검토해줘.

- 요구사항별 구현 여부(완료/부분/누락)를 표로 정리
- 코드를 다시 읽으며 예외 처리 누락, 무한 대기/크래시 가능 지점, API 키
  누락 시 동작, 종목 매핑 실패·가격 조회 실패 시 동작을 점검
- 실제로 앱을 구동해 문법 오류·임포트 오류가 없는지 확인하고, 가능하면
  브라우저로 열어 UI가 정상 렌더링되는지 확인
- 발견된 문제를 심각도와 함께 정리하고, 즉시 고칠 수 있는 것은 수정
```

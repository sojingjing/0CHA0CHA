# PRD: 매크로 테마 밸류체인(공급망) 탐색 에이전트

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (초안) |
| 작성일 | 2026-09-04 |
| 작성 목적 | Streamlit 기반 MVP 개발을 위한 요구사항 정의 |
| 상태 | Draft — 리뷰 후 구현 착수 |

---

## 1. 배경 및 문제 정의

특정 글로벌 이슈(예: "엔비디아 H200 수요 폭증")가 발생하면 관련 대장주(1차 수혜주)는 이미 시장에서 빠르게 반응해 급등하는 경우가 많다. 이 시점에 대장주를 매수하는 것은 리스크 대비 기대수익이 낮다. 반면 해당 이슈의 밸류체인(소재-부품-장비) 상에서 아직 시장의 관심을 받지 못한 2차, 3차 수혜주는 상대적으로 저평가 상태로 남아 있을 가능성이 있다.

이 에이전트는 이슈 → 밸류체인 구조 추론 → 종목 매핑 → 주가 급등 여부 필터링을 자동화하여, "아직 오르지 않은 수혜주 후보"를 빠르게 스크리닝하는 것을 목표로 한다.

## 2. 목표 (Goals)

- 사용자가 이슈 키워드를 입력하면, 해당 이슈와 연관된 산업 밸류체인 구조(소재→부품→장비→완제품/응용처)를 자동 생성한다.
- 밸류체인 상의 각 노드를 실제 상장 종목에 매핑한다.
- 이미 급등(기준: 최근 N일 대비 +15% 이상)한 종목은 "이미 반응 완료"로 간주하고 추천에서 제외한다.
- 대장주 대비 아직 반응하지 않은 2차, 3차 관계 종목을 우선순위로 제시한다.
- 모든 분석 근거(밸류체인 관계, 왜 수혜인지)를 함께 제시하여 사용자가 판단할 수 있도록 돕는다.

### Non-Goals (범위 제외)

- 실시간 자동매매/주문 실행 기능 없음.
- 투자 자문·매수/매도 시그널 확정 제공 아님 (참고용 스크리닝 도구).
- 해외 상장 종목(미국 등)의 완전한 커버리지는 MVP 범위 밖 — FinanceDataReader가 지원하는 국내(KRX) 종목 및 일부 해외 지수/종목 위주로 우선 구현.

## 3. 사용자 및 사용 시나리오

**대상 사용자**: 개인 투자자, 테마주 스크리닝을 빠르게 하고 싶은 리서처.

**시나리오 예시**
1. 사용자가 "엔비디아 H200 수요 폭증"을 입력한다.
2. 에이전트가 LLM을 통해 관련 밸류체인을 도출한다: HBM(소재/메모리) → 패키징/기판(부품) → 서버/장비(완제품) 등.
3. 밸류체인의 각 노드에 해당하는 국내 종목을 매핑한다 (예: HBM → SK하이닉스, 패키징 기판 → 이수페타시스 등).
4. 각 종목의 최근 주가 등락률을 조회한다.
5. 대장주(1차 수혜주)가 이미 15% 이상 급등했다면, 해당 종목은 표시만 하고 "이미 반응 완료"로 마킹, 추천 리스트에서는 제외한다.
6. 아직 급등하지 않은 2차/3차 종목을 "관심 후보"로 우선순위 정렬하여 제시한다.

## 4. 핵심 기능 요구사항 (Functional Requirements)

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F1 | 이슈 입력 | 텍스트 입력창에 글로벌 이슈 키워드/문장 입력 | Must |
| F2 | 밸류체인 구조 추론 | OpenAI ChatGPT 모델(.env의 API Key 사용)을 호출하여 이슈와 관련된 소재-부품-장비 밸류체인을 JSON 구조(계층/노드/관계)로 추출 | Must |
| F3 | 종목 매핑 | 밸류체인 각 노드를 실제 상장사명 및 종목코드로 매핑 (LLM 추론 + 필요 시 fdr.StockListing으로 종목코드 검증) | Must |
| F4 | GraphRAG 기반 관계 추론 | 밸류체인을 그래프(노드=기업/소재/부품, 엣지=공급·경쟁·응용 관계)로 구성하고, 대장주 노드로부터의 hop 거리(1차/2차/3차)를 계산하여 수혜 정도를 추론 | Must |
| F5 | 주가 데이터 조회 | FinanceDataReader(fdr)로 각 종목의 최근 주가 데이터 조회. 종목별 조회 사이 `time.sleep(1)` 적용 | Must |
| F6 | 급등 필터링 | 조회 기준일 대비 등락률이 +15% 이상인 종목은 "이미 급등" 태그를 달고 추천 리스트에서 제외 (표시는 하되 비활성/회색 처리) | Must |
| F7 | 수혜주 우선순위 추천 | 대장주가 급등한 경우, 같은 밸류체인의 미급등 2차/3차 종목을 hop 거리·관계 유형 기준으로 정렬하여 추천 | Must |
| F8 | 밸류체인 시각화 | 노드-엣지 그래프 또는 계층형 다이어그램으로 밸류체인과 각 종목의 등락률(색상 코딩)을 시각화 | Should |
| F9 | 분석 근거 표시 | 각 추천 종목에 대해 "왜 이 이슈의 수혜주인지"에 대한 LLM 생성 근거 텍스트 제공 | Should |
| F10 | 결과 내보내기 | 추천 결과를 CSV/표 형태로 다운로드 | Could |
| F11 | 조회 이력 캐싱 | 동일 이슈 재입력 시 API 재호출 최소화를 위한 세션 캐싱(st.cache_data) | Should |

## 5. 비기능 요구사항 (Non-Functional Requirements)

- **환경 변수 관리**: OpenAI API Key는 `.env` 파일(`OPENAI_API_KEY`)에서 `python-dotenv`로 로드. 코드에 하드코딩 금지, `.env`는 `.gitignore` 처리.
- **API Rate Limit 대응**: FinanceDataReader로 종목별 주가 조회 시 `time.sleep(1)` 적용하여 과도한 요청 방지.
- **에러 처리**: 
  - 특정 종목 코드 조회 실패(상장폐지, 코드 오류 등) 시 해당 종목만 스킵하고 전체 프로세스는 계속 진행.
  - OpenAI API 호출 실패 시 사용자에게 명확한 에러 메시지 표시 및 재시도 버튼 제공.
- **응답 시간/UX**: 종목 수가 많을 경우 `time.sleep(1)`로 인해 조회 시간이 길어질 수 있으므로 Streamlit `st.progress` 또는 `st.spinner`로 진행 상태 표시.
- **LLM 출력 안정성**: 밸류체인 구조는 LLM에 JSON 스키마를 강제(function calling 또는 response_format=json)하여 파싱 실패를 최소화.
- **보안**: 사용자 입력을 LLM 프롬프트에 그대로 삽입하지 않고 최소한의 정제(prompt injection 방지) 적용.

## 6. 시스템 아키텍처 (개략)

```
[사용자 입력: 이슈 텍스트]
        │
        ▼
[① LLM 호출: 밸류체인 구조 추출]  ← .env(OPENAI_API_KEY)
   출력: {노드(소재/부품/장비/응용처), 관계(공급/경쟁/고객)}
        │
        ▼
[② 종목 매핑]  ← LLM 추론 + fdr.StockListing 검증
   출력: 노드별 (종목명, 종목코드)
        │
        ▼
[③ GraphRAG 그래프 구성 + hop 거리 계산]
   (networkx 등으로 그래프화, 대장주 기준 1차/2차/3차 산출)
        │
        ▼
[④ 주가 조회 루프]  ← fdr.DataReader + time.sleep(1)
   출력: 종목별 최근 등락률
        │
        ▼
[⑤ 필터링: 등락률 +15% 이상 제외]
        │
        ▼
[⑥ 결과 정렬/시각화]  → Streamlit UI 출력
```

## 7. 화면 설계 (Streamlit UI 개요)

1. **사이드바**: 
   - 이슈 입력창 (text_input / text_area)
   - 급등 기준 임계값 슬라이더 (기본값 15%, 조정 가능)
   - 조회 기간 선택 (예: 최근 5거래일 / 20거래일)
   - 분석 시작 버튼
2. **메인 화면**
   - 상단: 입력 이슈 요약 및 LLM이 추론한 밸류체인 설명 텍스트
   - 밸류체인 시각화 영역 (그래프/다이어그램)
   - 결과 테이블: 종목명 | 종목코드 | 밸류체인 단계(1차/2차/3차) | 관계 설명 | 최근 등락률 | 상태(추천/제외-이미급등)
   - 추천 종목 하이라이트 카드 (근거 텍스트 포함)
   - 다운로드 버튼 (CSV)

## 8. 기술 스택

| 구분 | 기술 |
|---|---|
| 프레임워크 | Streamlit |
| LLM | OpenAI ChatGPT (Chat Completions / Responses API), `openai` Python SDK |
| 환경변수 | python-dotenv |
| 주가 데이터 | FinanceDataReader (fdr) |
| 그래프 처리 | networkx (GraphRAG 관계 그래프 구성 및 hop 거리 계산) |
| 데이터 처리 | pandas |
| 시각화 | plotly (그래프/차트), 또는 streamlit-agraph / graphviz (밸류체인 다이어그램) |

## 9. GraphRAG 적용 범위 (MVP 기준 명확화)

정식 Microsoft GraphRAG(커뮤니티 탐지, 임베딩 기반 요약 등)를 풀 구현하기보다, MVP에서는 **경량 GraphRAG 패턴**을 적용한다:

1. LLM이 이슈 텍스트로부터 (노드, 관계) 리스트를 구조화 추출.
2. 이를 `networkx` 그래프로 구성 (대장주 = root 노드).
3. 그래프 탐색(BFS)으로 root로부터의 hop 거리를 계산 → 1차(대장주)=0, 2차=1, 3차=2 등으로 매핑.
4. 필요 시 hop 거리 정보를 다시 LLM 프롬프트에 컨텍스트로 제공하여 "왜 수혜주인지" 설명을 생성 (RAG 스텝).

추후 고도화 시 임베딩 기반 유사 기업 탐색, 뉴스/공시 데이터 연동 등으로 확장 가능 (§11 로드맵 참고).

## 10. 데이터 소스 관련 제약사항

- FinanceDataReader는 주로 KRX(국내) 종목 및 일부 해외 지수/원자재를 지원. 해외 개별 종목(예: 미국 개별주) 데이터는 제한적일 수 있음 → MVP는 **국내 상장 종목 중심**으로 밸류체인 매핑.
- LLM이 제시하는 종목명이 비상장이거나 실제 종목코드와 불일치할 가능성 → `fdr.StockListing('KRX')` 등으로 종목명-코드 매칭 검증 단계 필요.
- 급등률 계산 기준일(이슈 발생일)을 사용자가 명확히 특정하지 않을 경우, 기본값으로 "최근 N거래일간 최고 등락률"을 사용.

## 11. 리스크 및 오픈 이슈

| 리스크 | 설명 | 대응 방안 |
|---|---|---|
| LLM 환각(hallucination) | 존재하지 않는 종목/관계를 생성할 위험 | 종목코드 검증 단계(F3)로 필터링, 검증 실패 종목은 UI에 "미확인" 표시 |
| API 비용/속도 | 종목 수 많을 시 fdr 호출이 `sleep(1)`로 인해 느려짐 | 진행률 표시, 종목 수 상한(예: 최대 20개) 설정 검토 |
| 투자 책임 이슈 | 추천이 투자 조언으로 오인될 소지 | UI 하단에 "참고용 정보이며 투자 판단은 본인 책임" 문구 명시 |
| 급등 기준의 단순성 | 단순 등락률만으로 "이미 반응 완료" 판단은 과단순화 가능 | 임계값을 사용자가 조정 가능하게(사이드바 슬라이더) 설계 |

## 12. 향후 로드맵 (MVP 이후)

- 뉴스/공시 크롤링 연동으로 밸류체인 근거 보강
- 해외 종목 데이터 소스 추가 연동
- 정식 GraphRAG(임베딩 기반 커뮤니티 탐지) 적용
- 백테스트: 과거 유사 이슈 발생 시 2차/3차 종목의 실제 후행 수익률 검증

## 13. 산출물 (다음 단계)

본 PRD 승인 후 아래 파일을 구현한다:
- `app.py` — Streamlit 메인 애플리케이션
- `requirements.txt` — 의존성 목록
- `.env.example` — 환경변수 템플릿 (`OPENAI_API_KEY=`)
